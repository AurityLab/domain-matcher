/**
 * Represents the pattern to match.
 *
 * A pattern can look like the following:
 * - "*.auritylab.com"
 * - "*.test.auritylab.com"
 * - "test.*.*auritylab.com"
 */
export class DomainPattern {
  private readonly pattern: RegExp

  constructor(pattern: string) {

  }

  private transformPattern(pattern: string): string {

  }
}
